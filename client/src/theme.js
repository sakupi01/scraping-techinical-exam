import { createTheme } from "@mui/system";

 export const theme = createTheme(
  {
    palette: {
      primary: {
        main: '#F25041',
      },
      secondary: {
        main: '#F2EDEB',
      },
    },
  }
 )
